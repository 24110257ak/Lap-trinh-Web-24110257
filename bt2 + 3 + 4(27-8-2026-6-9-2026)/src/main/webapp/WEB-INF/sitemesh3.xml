<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
    <!-- Thiết lập decorator prefix rỗng -->
    <decorator-prefix></decorator-prefix>

    <!-- Mappings for User Pages -->
    <mapping path="/profile" decorator="/decorators/web.jsp"/>
    <mapping path="/user/*" decorator="/decorators/web.jsp"/>
    <mapping path="/home" decorator="/decorators/web.jsp"/>
    <mapping path="/product" decorator="/decorators/web.jsp"/>
    <mapping path="/product/*" decorator="/decorators/web.jsp"/>

    <!-- Mappings for Admin Panel -->
    <mapping path="/admin/*" decorator="/decorators/admin.jsp"/>

    <!-- Excluded Paths -->
    <mapping path="/login" exclude="true"/>
    <mapping path="/register" exclude="true"/>
    <mapping path="/verify-otp" exclude="true"/>
    <mapping path="/forgot-password" exclude="true"/>
    <mapping path="/reset-password" exclude="true"/>
    <mapping path="/image" exclude="true"/>
</sitemesh>
